services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: codex_prime_qdrant
    ports:
      - "6333:6333"  # API port
      - "6334:6334"  # gRPC port
    volumes:
      - ./qdrant_data:/qdrant/storage